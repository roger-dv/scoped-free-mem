# C program using __attribute__((cleanup(__free_clean_up))) for scoped malloc() allocations

## Description

A simple C program that uses the `__attribute__((cleanup(xxx)))` feature to devise function locally scoped `malloc()` memory allocations - when the function exits then the `malloc` allocation is automatically freed due to code generated by the compiler.

This enables a C program to somewhat mimic C++ RAII.